<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.devinet">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.ParametreActivity"></activity>
        <activity android:name=".activities.Proposition" />
        <activity android:name=".activities.ResultatDetailActivity" />
        <activity android:name=".activities.ChoixListNiveau" />
        <activity android:name=".activities.SelectionNiveauActivity" />
        <activity android:name=".activities.AProposActivity" />
        <activity android:name=".activities.ProposerActivity" />
        <activity android:name=".activities.ResultatActivity" />
        <activity android:name=".activities.MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.JouerActivity" />
    </application>

</manifest>